{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
       <div class="row">
          <div class="col s6">
             <h2>{{ title }}</h2>
             <p>
                 {{ content }}
             </p>
          </div>
          <div class="col s6">
             <img src="{{ asset('uploads/images/posts/') ~ image }}" alt="{{ image }}">
          </div>
       </div>
    </div>

{% endblock %}